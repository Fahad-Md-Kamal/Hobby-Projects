version: '3.8'

services:
  nimbus-api:
    build:
      context: .
    ports: 
      - 8000:8000
    container_name: nimbus_api
    # image: nimbus_api/
    # command: >
    #   sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    command: /starte
    restart: always
    volumes:
      - ./:/app
